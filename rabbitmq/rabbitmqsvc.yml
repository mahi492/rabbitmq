apiVersion: v1
kind: Service
metadata:
  labels:
    app: rabbitmq
  name: rabbitmq
  namespace: default
spec:
  ports:
  - port: 800
    protocol: TCP
    targetPort: 800
  selector:
    app: rabbitmq
  type: NodePort
